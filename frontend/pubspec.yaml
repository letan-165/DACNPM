name: frontend
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_launcher_icons: ^0.13.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

# 🔥 cấu hình cho launcher icon (block mới)
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo.png"   # đặt logo tại đây

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
